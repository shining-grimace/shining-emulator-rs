
Done: Step 2h
Next: Step 2i

Consider animation:
  - The entire UI tree fading in and out on screen change
  - Drop-down options fading in and scaling in
  - The highlighted list row animating from one to the next
  - Scrolling content having an ease-out function rather than being instant

For later:
- I should ask the AI to check whether there are duplicated systems or components across the project and try to merge them where it's appropriate and reduces complexity or performance overhead
- I should structure code to make adding more languages easier later on
- Machine goes brrr: is vsync enabled?
- Remove all allow-dead-code annotations
- I should ask the AI if accessibility is important, and if so, what should be done
- I should ask AI if it's feasible to integrate libretro cores

For background image assets:
I'll try prompting like:
> Draw a <theme>-inspired image with <props>, plus <stylistic effects>, while being fairly minimal yet portraying emotions of <theme emotions>. There should be only a few objects in view, and quite close up. Draw in a 1990s SNES style, using various colour hues but with a very narrow color palette (such that gradients are not smooth but with noticeable graduations) typical of a GameBoy Color image. Draw in a resolution of 160 pixels wide by 144 pixels high.
